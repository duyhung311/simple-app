spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:28888
      name: gateway-service
      profile: development
      label: master
    loadbalancer:
      ribbon:
        enabled: false
